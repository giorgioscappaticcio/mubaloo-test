'use strict';

/**
 * @ngdoc directive
 * @name mubalootestApp.directive:inputdate
 * @description
 * # inputdate
 */
angular.module('mubalootestApp')
  .directive('inputdate', function () {
    return {
      restrict: 'AE',
      link: function postLink(scope, element, attrs) {
        
		// scope.removeclass = function(){
		// 	element.removeClass('placeholderclass');
		// }
		
      
      }
    };
  });
